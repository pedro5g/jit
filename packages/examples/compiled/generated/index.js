// Generated by jit — do not edit.
class JITValidationError extends Error {
  constructor(issues) {
    const first = issues[0];
    super(first ? (first.path ? first.path + ": " : "") + first.message : "validation failed");
    this.name = "JITValidationError";
    this.code = "VALIDATION_FAILED";
    this.issues = issues;
  }
}
const __hashCache = new WeakMap();
function __hashNumber(value) { return value | 0; }
function __hashBoolean(value) { return value ? 1 : 0; }
function __hashBigInt(value) { return Number(value & 0xffffffffn) | 0; }
function __hashString(value) {
  let hash = 0;
  for (let i = 0, len = value.length; i < len; i++) {
    hash = (hash * 31 + value.charCodeAt(i)) | 0;
  }
  return hash;
}
function __hashUnknown(value) {
  switch (typeof value) {
    case "string": return __hashString(value);
    case "number": return __hashNumber(value);
    case "boolean": return __hashBoolean(value);
    case "bigint": return __hashBigInt(value);
    case "undefined": return 0;
    case "symbol": return __hashString(String(value));
    case "object": return value === null ? 1 : __hashString(Object.prototype.toString.call(value));
    case "function": return __hashString("function");
  }
}
function __combineHash(left, right) { return ((left << 5) - left + right) | 0; }
const User_validator = /*#__PURE__*/ (() => {
  const __v0 = /^(?!\.)(?!.*\.\.)([A-Za-z0-9_'+\-.]*)[A-Za-z0-9_+-]@([A-Za-z0-9][A-Za-z0-9-]*\.)+[A-Za-z]{2,}$/;
  const __v1 = /^(?:(?:\d\d[2468][048]|\d\d[13579][26]|\d\d0[48]|[02468][048]00|[13579][26]00)-02-29|\d{4}-(?:(?:0[13578]|1[02])-(?:0[1-9]|[12]\d|3[01])|(?:0[469]|11)-(?:0[1-9]|[12]\d|30)|(?:02)-(?:0[1-9]|1\d|2[0-8])))T(?:(?:[01]\d|2[0-3]):[0-5]\d(?::[0-5]\d(?:\.\d+)?)?(?:Z))$/;
  const __v2 = /<(script|style|iframe|object|embed)[^>]*>[\s\S]*?<\/\1\s*>/gi;
  const __v3 = /<[^>]*>/g;
  const __v4 = /</g;
  const __v5 = />/g;
  function is(value) {
    let v1 = value;
    if (v1 === null || typeof v1 !== "object" || Array.isArray(v1)) {
      return false;
    }
    let v3 = v1.id;
    if (typeof v3 !== "number") {
      return false;
    }
    if (!Number.isInteger(v3) || v3 < -2147483648 || v3 > 2147483647) {
      return false;
    }
    if (v3 <= 0) {
      return false;
    }
    let v5 = v1.name;
    if (typeof v5 !== "string") {
      return false;
    }
    v5 = v5.trim();
    if (v5.length < 3) {
      return false;
    }
    if (v5.length > 80) {
      return false;
    }
    let v7 = v1.email;
    if (typeof v7 !== "string") {
      return false;
    }
    if (!__v0.test(v7)) {
      return false;
    }
    let v9 = v1.role;
    if (!((v9 === "admin") || (v9 === "member"))) {
      return false;
    }
    let v11 = v1.active;
    if (typeof v11 !== "boolean") {
      return false;
    }
    let v13 = v1.score;
    if (typeof v13 !== "number") {
      return false;
    }
    if (!Number.isFinite(v13) || Math.fround(v13) !== v13) {
      return false;
    }
    if (v13 < 0) {
      return false;
    }
    if (v13 > 100) {
      return false;
    }
    let v15 = v1.tags;
    if (!Array.isArray(v15)) {
      return false;
    }
    if (v15.length < 1) {
      return false;
    }
    if (v15.length > 8) {
      return false;
    }
    for (let i17 = 0; i17 < v15.length; i17++) {
      let v18 = v15[i17];
      if (typeof v18 !== "string") {
        return false;
      }
      if (v18.length < 2) {
        return false;
      }
    }
    let v20 = v1.createdAt;
    if (typeof v20 !== "string") {
      return false;
    }
    if (!__v1.test(v20)) {
      return false;
    }
    let v22 = v1.profile;
    if (v22 !== undefined) {
      if (v22 === null || typeof v22 !== "object" || Array.isArray(v22)) {
        return false;
      }
      let v24 = v22.bio;
      if (v24 !== null) {
        if (typeof v24 !== "string") {
          return false;
        }
        v24 = v24.replace(__v2, "").replace(__v3, "").replace(__v4, "&lt;").replace(__v5, "&gt;");
      }
    }
    return true;
  }
  function safeParse(value) {
    const issues = [];
    let v1 = value;
    let o2 = v1;
    let b3;
    if (v1 === null || typeof v1 !== "object" || Array.isArray(v1)) {
      issues[issues.length] = { path: "", code: "expected_object", expected: "object", message: "expected object", received: typeof v1 };
    } else {
      let v4 = v1.id;
      let o5 = v4;
      if (typeof v4 !== "number") {
        issues[issues.length] = { path: "id", code: "expected_number", expected: "number", message: "expected number", received: typeof v4 };
      } else {
        if (!Number.isInteger(v4) || v4 < -2147483648 || v4 > 2147483647) {
          issues[issues.length] = { path: "id", code: "not_int32", expected: "int32", message: "expected a 32-bit signed integer" };
        }
        if (v4 <= 0) {
          issues[issues.length] = { path: "id", code: "not_positive", expected: "> 0", message: "expected a positive number" };
        }
      }
      o5 = v4;
      let v6 = v1.name;
      let o7 = v6;
      if (typeof v6 !== "string") {
        issues[issues.length] = { path: "name", code: "expected_string", expected: "string", message: "expected string", received: typeof v6 };
      } else {
        v6 = v6.trim();
        if (v6.length < 3) {
          issues[issues.length] = { path: "name", code: "too_small", expected: "length >= 3", message: "expected at least 3 characters" };
        }
        if (v6.length > 80) {
          issues[issues.length] = { path: "name", code: "too_big", expected: "length <= 80", message: "expected at most 80 characters" };
        }
      }
      o7 = v6;
      let v8 = v1.email;
      let o9 = v8;
      if (typeof v8 !== "string") {
        issues[issues.length] = { path: "email", code: "expected_string", expected: "string", message: "expected string", received: typeof v8 };
      } else {
        if (!__v0.test(v8)) {
          issues[issues.length] = { path: "email", code: "invalid_format", expected: "email", message: "expected a valid email" };
        }
      }
      o9 = v8;
      let v10 = v1.role;
      let o11 = v10;
      if (!((v10 === "admin") || (v10 === "member"))) {
        issues[issues.length] = { path: "role", code: "invalid_union", expected: "union", message: "value matched no union option" };
      }
      o11 = v10;
      let v12 = v1.active;
      let o13 = v12;
      if (typeof v12 !== "boolean") {
        issues[issues.length] = { path: "active", code: "expected_boolean", expected: "boolean", message: "expected boolean" };
      }
      o13 = v12;
      let v14 = v1.score;
      let o15 = v14;
      if (typeof v14 !== "number") {
        issues[issues.length] = { path: "score", code: "expected_number", expected: "number", message: "expected number", received: typeof v14 };
      } else {
        if (!Number.isFinite(v14) || Math.fround(v14) !== v14) {
          issues[issues.length] = { path: "score", code: "not_float32", expected: "float32", message: "expected a float32-representable number" };
        }
        if (v14 < 0) {
          issues[issues.length] = { path: "score", code: "too_small", expected: ">= 0", message: "expected a number >= 0" };
        }
        if (v14 > 100) {
          issues[issues.length] = { path: "score", code: "too_big", expected: "<= 100", message: "expected a number <= 100" };
        }
      }
      o15 = v14;
      let v16 = v1.tags;
      let o17 = v16;
      if (!Array.isArray(v16)) {
        issues[issues.length] = { path: "tags", code: "expected_array", expected: "array", message: "expected array", received: typeof v16 };
      } else {
        if (v16.length < 1) {
          issues[issues.length] = { path: "tags", code: "too_small", expected: "length >= 1", message: "expected at least 1 items" };
        }
        if (v16.length > 8) {
          issues[issues.length] = { path: "tags", code: "too_big", expected: "length <= 8", message: "expected at most 8 items" };
        }
        for (let i18 = 0; i18 < v16.length; i18++) {
          let v19 = v16[i18];
          let o20 = v19;
          if (typeof v19 !== "string") {
            issues[issues.length] = { path: "tags[" + i18 + "]", code: "expected_string", expected: "string", message: "expected string", received: typeof v19 };
          } else {
            if (v19.length < 2) {
              issues[issues.length] = { path: "tags[" + i18 + "]", code: "too_small", expected: "length >= 2", message: "expected at least 2 characters" };
            }
          }
          o20 = v19;
        }
      }
      o17 = v16;
      let v21 = v1.createdAt;
      let o22 = v21;
      if (typeof v21 !== "string") {
        issues[issues.length] = { path: "createdAt", code: "expected_string", expected: "string", message: "expected string", received: typeof v21 };
      } else {
        if (!__v1.test(v21)) {
          issues[issues.length] = { path: "createdAt", code: "invalid_format", expected: "datetime", message: "expected a valid datetime" };
        }
      }
      o22 = v21;
      let v23 = v1.profile;
      let o24 = v23;
      if (v23 !== undefined) {
        let b25;
        if (v23 === null || typeof v23 !== "object" || Array.isArray(v23)) {
          issues[issues.length] = { path: "profile", code: "expected_object", expected: "object", message: "expected object", received: typeof v23 };
        } else {
          let v26 = v23.bio;
          let o27 = v26;
          if (v26 !== null) {
            if (typeof v26 !== "string") {
              issues[issues.length] = { path: "profile.bio", code: "expected_string", expected: "string", message: "expected string", received: typeof v26 };
            } else {
              v26 = v26.replace(__v2, "").replace(__v3, "").replace(__v4, "&lt;").replace(__v5, "&gt;");
            }
            o27 = v26;
          }
          b25 = { "bio": o27 };
        }
        if (b25 === undefined) { b25 = v23; }
        o24 = b25;
      }
      b3 = { "id": o5, "name": o7, "email": o9, "role": o11, "active": o13, "score": o15, "tags": o17, "createdAt": o22, "profile": o24 };
    }
    if (b3 === undefined) { b3 = v1; }
    o2 = b3;
    if (issues.length !== 0) {
      return { success: false, issues: issues };
    }
    return { success: true, data: o2 };
  }
  return { is: is, safeParse: safeParse };
})();
const User_is = /*#__PURE__*/ ((v) => v.is)(User_validator);
const User_safeParse = /*#__PURE__*/ ((v) => v.safeParse)(User_validator);
const User_parse = /*#__PURE__*/ ((v) => (value) => { const r = v.safeParse(value); if (r.success) return r.data; throw new JITValidationError(r.issues); })(User_validator);
const User_fromJSON = /*#__PURE__*/ ((parse) => (json) => parse(JSON.parse(json)))(User_parse);
const User_hash = /*#__PURE__*/ (() => {
  const compute = (function hash(value) {
    let h = 23;
    {
    let h_1 = __hashNumber(value.id);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashString(value.name);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashString(value.email);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashUnknown(value.role);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashBoolean(value.active);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashNumber(value.score);
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = 17;
    for (let i = 0, len = value.tags.length; i < len; i++) {
      let h_1_1 = __hashString(value.tags[i]);
      h_1 = __combineHash(h_1, h_1_1);
    }
      h = __combineHash(h, h_1);
    }
    {
    let h_1 = __hashString(value.createdAt);
      h = __combineHash(h, h_1);
    }
    {
    let h_1;
    if (value.profile === undefined) {
      h_1 = 0;
    } else {
      let h_1_1 = 23;
      {
      let h_1_1_2;
      if (value.profile.bio === null) {
        h_1_1_2 = 1;
      } else {
        let h_1_1_2_2 = __hashString(value.profile.bio);
        h_1_1_2 = h_1_1_2_2;
      }
        h_1_1 = __combineHash(h_1_1, h_1_1_2);
      }
      h_1 = h_1_1;
    }
      h = __combineHash(h, h_1);
    }
    return h;
  });
  return (value) => {
    if ((typeof value === "object" && value !== null) || typeof value === "function") {
      const cached = __hashCache.get(value);
      if (cached !== undefined) return cached;
      const hash = compute(value);
      __hashCache.set(value, hash);
      return hash;
    }
    return compute(value);
  };
})();
const User_equal = (function equal(l, r) {
  if (l === r) {
    return true;
  }
  if (l.id !== r.id && (l.id === l.id || r.id === r.id)) {
    return false;
  }
  if (l.name !== r.name) {
    return false;
  }
  if (l.email !== r.email) {
    return false;
  }
  if (l.role !== r.role && (l.role === l.role || r.role === r.role)) {
    return false;
  }
  if (l.active !== r.active) {
    return false;
  }
  if (l.score !== r.score && (l.score === l.score || r.score === r.score)) {
    return false;
  }
  const l_tags = l.tags;
  const r_tags = r.tags;
  const len = l_tags.length;
  if (len !== r_tags.length) {
    return false;
  }
  for (let i = len; i-- !== 0;) {
    if (l_tags[i] !== r_tags[i]) {
      return false;
    }
  }
  if (l.createdAt !== r.createdAt) {
    return false;
  }
  const l_profile = l.profile;
  const r_profile = r.profile;
  if (!Object.is(l_profile, r_profile)) {
    if (((l_profile === undefined) || (r_profile === undefined))) {
      return false;
    }
    if (!Object.is(l_profile.bio, r_profile.bio)) {
      if (l_profile.bio !== r_profile.bio) {
        return false;
      }
      if (((l_profile.bio === null) || (r_profile.bio === null))) {
        return false;
      }
    }
  }
  return true;
});
const User_clone = (function clone(value) {
  const len = value.tags.length;
  const out_tags = new Array(len);
  for (let i = 0; i < len; i++) {
    out_tags[i] = value.tags[i];
  }
  let out_profile = value.profile;
  if (value.profile !== undefined) {
    let out_profile_inner_bio = value.profile.bio;
    if (value.profile.bio !== null) {
      const out_profile_inner_bio_inner = value.profile.bio;
      out_profile_inner_bio = out_profile_inner_bio_inner;
    }
    const out_profile_inner = { "bio": out_profile_inner_bio };
    out_profile = out_profile_inner;
  }
  const out = { id: value.id, name: value.name, email: value.email, role: value.role, active: value.active, score: value.score, "tags": out_tags, createdAt: value.createdAt, "profile": out_profile };
  return out;
});
const User_diff = (function diff(left, right) {
  const changes = [];
  if (Object.is(left, right)) {
    return changes;
  }
  if (!Object.is(left, right)) {
    if (!Object.is(left.id, right.id)) {
      changes[changes.length] = { type: "update", path: ["id"], value: right.id };
    }
    if (!Object.is(left.name, right.name)) {
      changes[changes.length] = { type: "update", path: ["name"], value: right.name };
    }
    if (!Object.is(left.email, right.email)) {
      changes[changes.length] = { type: "update", path: ["email"], value: right.email };
    }
    if (!Object.is(left.role, right.role)) {
      changes[changes.length] = { type: "update", path: ["role"], value: right.role };
    }
    if (!Object.is(left.active, right.active)) {
      changes[changes.length] = { type: "update", path: ["active"], value: right.active };
    }
    if (!Object.is(left.score, right.score)) {
      changes[changes.length] = { type: "update", path: ["score"], value: right.score };
    }
    if (!Object.is(left.tags, right.tags)) {
      const leftLen = left.tags.length;
      const rightLen = right.tags.length;
      const commonLen = leftLen < rightLen ? leftLen : rightLen;
      for (let i = 0; i < commonLen; i++) {
        if (!Object.is(left.tags[i], right.tags[i])) {
          changes[changes.length] = { type: "update", path: ["tags", i], value: right.tags[i] };
        }
      }
      for (let i = commonLen; i < rightLen; i++) {
        changes[changes.length] = { type: "add", path: ["tags", i], value: right.tags[i] };
      }
      for (let i = commonLen; i < leftLen; i++) {
        changes[changes.length] = { type: "remove", path: ["tags", i] };
      }
    }
    if (!Object.is(left.createdAt, right.createdAt)) {
      changes[changes.length] = { type: "update", path: ["createdAt"], value: right.createdAt };
    }
    if (!Object.is(left.profile, right.profile)) {
      if (!(left.profile !== undefined) || !(right.profile !== undefined)) {
        changes[changes.length] = { type: "update", path: ["profile"], value: right.profile };
      } else {
        if (!Object.is(left.profile, right.profile)) {
          if (!Object.is(left.profile.bio, right.profile.bio)) {
            if (!(left.profile.bio !== null) || !(right.profile.bio !== null)) {
              changes[changes.length] = { type: "update", path: ["profile", "bio"], value: right.profile.bio };
            } else {
              if (!Object.is(left.profile.bio, right.profile.bio)) {
                changes[changes.length] = { type: "update", path: ["profile", "bio"], value: right.profile.bio };
              }
            }
          }
        }
      }
    }
  }
  return changes;
});
const User_stringify = ((function () {
  const __se = /[\u0000-\u001f\u0022\u005c\ud800-\udfff]/;
  function str(value) {
    const len = value.length;
    if (len < 42) {
      for (let i = 0; i < len; i++) {
        const code = value.charCodeAt(i);
        if (code < 32 || code === 34 || code === 92 || (code > 55295 && code < 57344)) {
          return JSON.stringify(value);
        }
      }
      return '"' + value + '"';
    }
    if (__se.test(value)) return JSON.stringify(value);
    return '"' + value + '"';
  }
  function stringify(value) {
    let s = "";
    s += "{";
    s += "\"id\":";
    s += Number.isFinite(value.id) ? "" + value.id : "null";
    s += ",\"name\":";
    s += str(value.name);
    s += ",\"email\":";
    s += str(value.email);
    s += ",\"role\":";
    s += JSON.stringify(value.role) ?? "null";
    s += ",\"active\":";
    s += value.active ? "true" : "false";
    s += ",\"score\":";
    s += Number.isFinite(value.score) ? "" + value.score : "null";
    s += ",\"tags\":";
    const v1 = value.tags;
    s += "[";
    for (let i2 = 0; i2 < v1.length; i2++) {
      if (i2 !== 0) s += ",";
      const e3 = v1[i2];
      s += str(e3);
    }
    s += "]";
    s += ",\"createdAt\":";
    s += str(value.createdAt);
    if (value.profile !== undefined) {
      s += ",\"profile\":";
      if (value.profile == null) {
        s += "null";
      } else {
        const v4 = value.profile;
        s += "{";
        s += "\"bio\":";
        if (v4.bio == null) {
          s += "null";
        } else {
          s += str(v4.bio);
        }
        s += "}";
      }
    }
    s += "}";
    return s;
  }
  return stringify;
})());
const User_mask = (function scrub(value) {
  const s1 = value.email;
  return { "id": value.id, "name": value.name, "email": (s1.length > 4 ? "***" + s1.slice(-4) : "***"), "role": value.role, "active": value.active, "score": value.score, "tags": value.tags, "createdAt": value.createdAt, "profile": value.profile };
});
const User_sanitize = /*#__PURE__*/ (() => {
  const __scriptBlocks = /<(script|style|iframe|object|embed)[^>]*>[\s\S]*?<\/\1\s*>/gi;
  const __htmlTags = /<[^>]*>/g;
  const __htmlTagParts = /<\s*(\/?)\s*([A-Za-z][A-Za-z0-9-]*)(?:\s[^>]*)?>/g;
  const __amp = /&/g;
  const __lt = /</g;
  const __gt = />/g;
  const __quote = /"/g;
  const __apostrophe = /'/g;
  const __controls = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F]/g;
  const __sqlIdentifier = /^[^A-Za-z_$]+|[^A-Za-z0-9_$]+/g;
  const __pathTraversal = /\.\.+/g;
  const __pathSegment = /[\\/:*?"<>|]/g;
  return (function scrub(value) {
    const s1 = value.profile;
    let r2 = s1;
    if (s1 !== undefined) {
      const s3 = s1.bio;
      let r4 = s3;
      if (s3 !== null) {
        r4 = s3.replace(__scriptBlocks, "").replace(__htmlTags, "").replace(__lt, "&lt;").replace(__gt, "&gt;");
      }
      r2 = { "bio": r4 };
    }
    return { "id": value.id, "name": value.name, "email": value.email, "role": value.role, "active": value.active, "score": value.score, "tags": value.tags, "createdAt": value.createdAt, "profile": r2 };
  });
})();
const User_codec = /*#__PURE__*/ (() => {
  const __enc = new TextEncoder();
  const __dec = new TextDecoder();
  function _write(value, u8, dv, o) {
    let m1 = 0;
    if (value.profile === null) m1 |= 1; else if (value.profile !== undefined) m1 |= 2;
    dv.setUint8(o, m1); o += 1;
    dv.setFloat64(o, value.id, true); o += 8;
    const w2 = __enc.encodeInto(value.name, u8.subarray(o + 4));
    if (w2.read !== value.name.length) {
      throw new RangeError("jit codec: target buffer too small");
    }
    dv.setUint32(o, w2.written, true); o += 4 + w2.written;
    const w3 = __enc.encodeInto(value.email, u8.subarray(o + 4));
    if (w3.read !== value.email.length) {
      throw new RangeError("jit codec: target buffer too small");
    }
    dv.setUint32(o, w3.written, true); o += 4 + w3.written;
    const v4 = value.role;
    if (v4 === "admin") {
      dv.setUint8(o, 0); o += 1;
    } else if (v4 === "member") {
      dv.setUint8(o, 1); o += 1;
    } else {
      throw new RangeError("jit codec: value matched no union option");
    }
    dv.setUint8(o, value.active ? 1 : 0); o += 1;
    dv.setFloat64(o, value.score, true); o += 8;
    const v5 = value.tags;
    dv.setUint32(o, v5.length, true); o += 4;
    for (let i6 = 0; i6 < v5.length; i6++) {
      const e7 = v5[i6];
      const w8 = __enc.encodeInto(e7, u8.subarray(o + 4));
      if (w8.read !== e7.length) {
        throw new RangeError("jit codec: target buffer too small");
      }
      dv.setUint32(o, w8.written, true); o += 4 + w8.written;
    }
    const w9 = __enc.encodeInto(value.createdAt, u8.subarray(o + 4));
    if (w9.read !== value.createdAt.length) {
      throw new RangeError("jit codec: target buffer too small");
    }
    dv.setUint32(o, w9.written, true); o += 4 + w9.written;
    if (value.profile != null) {
      const v10 = value.profile;
      let m11 = 0;
      if (v10.bio === null) m11 |= 1; else if (v10.bio !== undefined) m11 |= 2;
      dv.setUint8(o, m11); o += 1;
      if (v10.bio != null) {
        const w12 = __enc.encodeInto(v10.bio, u8.subarray(o + 4));
        if (w12.read !== v10.bio.length) {
          throw new RangeError("jit codec: target buffer too small");
        }
        dv.setUint32(o, w12.written, true); o += 4 + w12.written;
      }
    }
    return o;
  }
  function encode(value) {
    let size = 1;
    size += 1;
    size += 8;
    size += 4 + value.name.length * 3;
    size += 4 + value.email.length * 3;
    const v13 = value.role;
    size += 1;
    if (v13 === "admin") {
    } else if (v13 === "member") {
    } else {
      throw new RangeError("jit codec: value matched no union option");
    }
    size += 1;
    size += 8;
    const v14 = value.tags;
    size += 4;
    for (let i15 = 0; i15 < v14.length; i15++) {
      const e16 = v14[i15];
      size += 4 + e16.length * 3;
    }
    size += 4 + value.createdAt.length * 3;
    if (value.profile != null) {
      const v17 = value.profile;
      size += 1;
      if (v17.bio != null) {
        size += 4 + v17.bio.length * 3;
      }
    }
    const buf = new ArrayBuffer(size);
    const dv = new DataView(buf);
    const u8 = new Uint8Array(buf);
    u8[0] = 1;
    return u8.subarray(0, _write(value, u8, dv, 1));
  }
  function encodeInto(value, target) {
    if (!(target instanceof Uint8Array)) {
      throw new TypeError("jit codec: encodeInto target must be a Uint8Array");
    }
    if (target.length < 1) throw new RangeError("jit codec: target buffer too small");
    const dv = new DataView(target.buffer, target.byteOffset, target.byteLength);
    target[0] = 1;
    return _write(value, target, dv, 1);
  }
  function decode(input) {
    const u8 = input instanceof Uint8Array ? input : new Uint8Array(input);
    const dv = new DataView(u8.buffer, u8.byteOffset, u8.byteLength);
    if (u8.length < 1 || u8[0] !== 1) {
      throw new RangeError("jit codec: schema version mismatch: expected 1, got " + (u8.length < 1 ? "empty buffer" : u8[0]));
    }
    let o = 1;
    const m18 = dv.getUint8(o); o += 1;
    const n19 = dv.getFloat64(o, true); o += 8;
    const l20 = dv.getUint32(o, true); o += 4;
    if (o + l20 > u8.length) throw new RangeError("jit codec: truncated buffer");
    const t21 = __dec.decode(u8.subarray(o, o + l20)); o += l20;
    const l22 = dv.getUint32(o, true); o += 4;
    if (o + l22 > u8.length) throw new RangeError("jit codec: truncated buffer");
    const t23 = __dec.decode(u8.subarray(o, o + l22)); o += l22;
    const t24 = dv.getUint8(o); o += 1;
    let r25;
    if (t24 === 0) {
      r25 = "admin";
    } else if (t24 === 1) {
      r25 = "member";
    } else {
      throw new RangeError("jit codec: invalid union tag: " + t24);
    }
    const b26 = dv.getUint8(o) !== 0; o += 1;
    const n27 = dv.getFloat64(o, true); o += 8;
    const l28 = dv.getUint32(o, true); o += 4;
    const a29 = new Array(l28);
    for (let i30 = 0; i30 < l28; i30++) {
      const l31 = dv.getUint32(o, true); o += 4;
      if (o + l31 > u8.length) throw new RangeError("jit codec: truncated buffer");
      const t32 = __dec.decode(u8.subarray(o, o + l31)); o += l31;
      a29[i30] = t32;
    }
    const l33 = dv.getUint32(o, true); o += 4;
    if (o + l33 > u8.length) throw new RangeError("jit codec: truncated buffer");
    const t34 = __dec.decode(u8.subarray(o, o + l33)); o += l33;
    const s35 = (m18 >> 0) & 3;
    let r36;
    if (s35 === 1) {
      r36 = null;
    } else if (s35 === 2) {
      const m37 = dv.getUint8(o); o += 1;
      const s38 = (m37 >> 0) & 3;
      let r39;
      if (s38 === 1) {
        r39 = null;
      } else if (s38 === 2) {
        const l40 = dv.getUint32(o, true); o += 4;
        if (o + l40 > u8.length) throw new RangeError("jit codec: truncated buffer");
        const t41 = __dec.decode(u8.subarray(o, o + l40)); o += l40;
        r39 = t41;
      }
      r36 = { "bio": r39 };
    }
    return { "id": n19, "name": t21, "email": t23, "role": r25, "active": b26, "score": n27, "tags": a29, "createdAt": t34, "profile": r36 };
  }
  return { encode: encode, encodeInto: encodeInto, decode: decode };
})();
const User_findActiveAdmins = /*#__PURE__*/ (() => {
  const __q0 = "admin";
  const __q1 = true;
  return (function query(value) {
  const len = value.length;
  const out = new Array(len);
  let j = 0;
  for (let i = 0; i < len; i++) {
    const item = value[i];
    if ((item.role === __q0) && (item.active === __q1)) {
      out[j++] = { "id": item.id, "name": item.name, "score": item.score };
    }
  }
  out.length = j;
  return out;
});
})();
const User_toPublicUser = /*#__PURE__*/ (() => {
  return ({
  map: function map(source) {
    return { "id": source.id, "name": source.name, "role": source.role };
  }
});
})();
const User = /*#__PURE__*/ Object.freeze({
  is: User_is,
  safeParse: User_safeParse,
  parse: User_parse,
  fromJSON: User_fromJSON,
  hash: User_hash,
  equal: User_equal,
  clone: User_clone,
  diff: User_diff,
  stringify: User_stringify,
  mask: User_mask,
  sanitize: User_sanitize,
  codec: User_codec,
  findActiveAdmins: User_findActiveAdmins,
  toPublicUser: User_toPublicUser,
});

const iterateActiveUsers = /*#__PURE__*/ (() => {
  const __q0 = true;
  return ((function() {
function* stage0(input, params) {
  let count2 = 0;
  if (Array.isArray(input)) {
    for (let i = 0, len = input.length; i < len; i++) {
      const item = input[i];
      let output = item;
      if (!(item.active === __q0)) continue;
      output = { "id": item.id, "name": item.name };
      if (count2++ === 10) return;
      yield output;
    }
    return;
  }
  for (const item of input) {
    let output = item;
    if (!(item.active === __q0)) continue;
    output = { "id": item.id, "name": item.name };
    if (count2++ === 10) return;
    yield output;
  }
}
function query(input) {
  return stage0(input, undefined);
}
return query;
})());
})();
const visitActiveUsers = /*#__PURE__*/ (() => {
  const __q0 = true;
  return ((function () {
function visit(input, consume) {
  let emitted = 0;
  if (Array.isArray(input)) {
    for (let i = 0, len = input.length; i < len; i++) {
      const item = input[i];
      let output = item;
      if (!(item.active === __q0)) continue;
      output = { "id": item.id };
      consume(output);
      emitted++;
    }
    return emitted;
  }
  for (const item of input) {
    let output = item;
    if (!(item.active === __q0)) continue;
    output = { "id": item.id };
    consume(output);
    emitted++;
  }
  return emitted;
}
return visit;
})());
})();
export { User, iterateActiveUsers, visitActiveUsers };
